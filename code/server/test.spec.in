% Note that all instances of @PATH@ get turned into the appropriate $(pwd)
% by the buildfiles at compile-time.


% {cover, "@PATH@/tests/common_test/coverage.config"}.
{logdir, "@PATH@/.common_test/logs"}.
{include, "@PATH@/include/"}.


{alias, test_dir, "@PATH@/tests/common_test/"}.
{suites, test_dir, all}.
